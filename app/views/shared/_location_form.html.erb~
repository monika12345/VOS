<%= form_for(@location) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.select :city , options_for_select(@locations) %>

  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
